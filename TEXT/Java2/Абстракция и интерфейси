Абстракция и интерфейси
1.Абстракция- да извадим проперти от целия клас 
2.Интерфейс 
в него се пишат неимплементирани методи
имплементира се с public class DayofWeekRun implements IMovable
default void  - метод в интерфесй който не чуи йерархията 
3.Абстрактни класове
Това е като клас Shape
метод деклариран с думата abstract
abstract class GraphicObject {
    int x, y;
    ...
    void moveTo(int newX, int newY) {
        ...
    }
    abstract void draw();
    abstract void resize();
}

   ИНТЕРФЕЙСИ
Вместо клас се прави interface вички методи вътре са апстрактни
Абстрактни интерфейс
в интерфейса се дефинират само константи и абстрактни методи
implements - един интерфейс наследява друг
Всички АБСТРАКТНИ методи които имплементира интерфейса  
Default методите в интерфейс могат да бъдат имплементирани
default void (){
} и вече е позволено да се имплементира

Може да се декларират и static метод и двата извикаме на класа

Abstract vs Interface
1.Abstract-